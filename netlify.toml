# Netlify configuration for Eat Istanbul (static site, no build step)
[build]
  publish = "."

# Friendly headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
